# wanctl web 重构 · 交接

last-reviewed: 2026-09-04

## 0. 已上线

**https://wc.z10.dev**（橙云，分享用）与 **https://wc.lab.z10.dev**（灰云，大陆直连）
是同一个站，2026-09-04 14:40 上线。发布跑 `tools/deploy.sh`，不用手工 tar。
状态：首屏与「安全模型」一屏都已获甲方认可（安全模型：「很对味，很干净，很极简」）。
**09-04 下午重做了首屏的交互呈现**——起因是甲方落地时把那块活 demo 当成了一张图片。
往下的章节（自部署 / 文档入口）没开工。

工作区：worktree `~/projects/wanctl-oss-worktrees/web-tagout`，分支 `web-tagout`，
从 `main@1d4327c` 开出。主工作区 `~/projects/wanctl-oss` 保持只读。
提交：`ee49939` → `7460c8c` → `5a9988a`（当前）。远端还没推。

---

## 1. 甲方已拍板的（别再问）

| 决策 | 结论 |
|---|---|
| 官网落点 | **`wc.z10.dev`**。不买新域名，门户 `wanctl.z10.dev` 原地不动、已入网设备零迁移 |
| 语言 | **英文为主 + 中文可切**（仓库 README 已是英文，GitHub 来的人默认英文） |
| 扫码即成设备 demo | **v1 不做，留 v2**。它需要在生产 relay 上开长期暴露的公开 demo 命名空间 |
| 视觉方向 | **苹果式极简**，见 `DESIGN.md`。初稿已认可 |
| 演示数据 | **全部虚构**。官网不渲染任何真实部署的机器名、域名、延迟、路径 |

## 2. 被否掉的路线（**不要重走**，每条都烧过一轮）

1. **四个深色技术方向**（舰桥 linear / 终端 tty / 拓扑 sanity / 面板 nintendo-2001）——
   甲方：「全被我 pass 了，做的都很差，ai 味很浓」。
   病根：三个参照站求最大公约数 = 把所有特征减掉。产物在 `portal-directions` worktree。
2. **挂牌上锁 LOTO**（冷白搪瓷 + 安全黄）——语义映射极准，但甲方判「太冷、太常见」。
   代码在 `ee49939` / `7460c8c`，`DESIGN.md` 第 10 节记了为什么留着这段历史。
3. **高密度构图**（满屏板墙 / 密集仪表 / 数据即画面）——**触发甲方密集恐惧症**，硬红线。
4. **锁 / 机房 / 精密硬件**这一族——「都很冷也很常见」。
5. **发散母题**（印 / 塔台进程条 / 园子 / 折纸信笺 / 总谱）——出过图，甲方未选，
   收敛到苹果。图都在 `/private/tmp/.../scratchpad/review4/`（会随 session 清掉，
   要留就先拷出来）。

**关键教训（写进了 DESIGN.md 第 2 节）**：
「空」和「极简」的差别不在留白多少，在于**中央有没有主体镇场**。
上一版是小卡片飘在白底上 → 留白塌成空洞 → 我去堆密度补 → 撞红线。两个批评是同一个病。

## 3. 现在长什么样

`site/` 全站 332K（含 4 个自托管 woff2）。零构建、零依赖、纯静态。

- `site/index.html` — 七段：首屏（白，海报）→ **命题·审批凭证（深，chapter）**
  → 设备列表（浅灰，minor）→ 身份核对（深，居中打断）→ 安装（白，海报）
  → **两扇门（浅灰，50/50）** → 页脚（白）
- `site/assets/app.css` — 苹果语法：单一 Action Blue、字号阶 56/40/17、负字距、
  无 500 字重、整幅 tile 颜色变化即分隔线、唯一投影只给三个「产品渲染」
- `site/assets/app.js` — 首屏双屏里跑的活 demo（终端 + 手机审批），`window.__demo` 验收接口
- `site/assets/fonts/` — Schibsted Grotesk（展示/正文）+ JetBrains Mono（标识符）。
  **必须自托管**：ls 在大陆，`fonts.googleapis.com` 对访客不可靠。
  `archivo-*.woff2` 是 LOTO 版遗留，现已无引用，**下次可删**

主体解法（这是本轮最关键的设计判断）：**苹果的语言是摄影优先的，而 wanctl 没有硬件可拍。**
不编造不存在的产品，改用 CSS 画笔记本和手机外壳、**里面跑真实的活界面**。

## 3.5 首屏为什么会被当成图片，怎么修的（09-04）

甲方原话：「一进来这块是静态的我差点以为是图片了……其他用户看到这些肯定也会以为是图片」。

**根因**：`boot()` 走的是 instant 分支，落地时命令已经画完、连光标都不闪 ——
**零运动**。而设备外壳恰恰是「这是营销配图」最强的先验信号，两件事叠在一起就成了图片。

三层修法，全部已上：

1. **手机会亮一下**。开场手机屏是黑的 → 笔记本带光标敲出命令 → 终端开始等待 →
   手机亮起、请求升上来。这就是 `DESIGN.md` 第 1 节那句场景句，之前只写在纸上。
2. **等待是有时长的**：`waiting for the owner…  0:07`，每秒跳一次。会走字的东西不可能
   是照片，而且它是真的（`wanctl exec` 真的阻塞在那儿）。十秒没人动，agent 自己再补一句
   `still waiting — it's your call`，不给页面加新元素。
3. **「该你了」有指向**：主按钮每 3.4 秒轻呼吸两下；手机放大到与笔记本等高
   （184px 宽），屏内字号同比放大；中段补上请求方的短指纹
   （`SHA256:fJoz5aA…DOAEo=`，形状同 `transport.ShortFingerprint`），
   动作区上方加一道分隔线。

后续每一轮请求也会「动一下」（内容升起 + 手机抬一下），
**但命中已签规则直接放行的那一轮不动** —— 动=需要你，不动=不需要你。

期间修掉三个只有真浏览器才会暴露的问题：

- 手机上的秒表 `pend.textContent = …` 把那颗活体指示点一起抹掉了（第一次跳动就没了）。
- 浏览器允许在连字符后折行，`--epochs` 被断成 `--` / `epochs`，读起来像两个参数。
  每个 token 各自 `nowrap`，超过 16 字的长 token（`--query-gpu=memory.used`）才放开内部断行。
- 手机放大后整个舞台掉到折线以下，那颗蓝按钮反而看不见了。首屏上留白收窄
  （`.tile.hero`）+ 舞台上边距收窄，现在 1440×900 下按钮落在 613–649px。

## 3.6 安装屏的真实 URL（09-04）

上一版页面上写的 `https://wanctl.dev/install.sh` **是我编的，那个域名不存在**。
现在四条命令全部核过，其中镜像那条是 `curl` 实测 200 的：

| | GitHub | 中国镜像（官方 relay） |
|---|---|---|
| macOS · Linux | `curl -fsSL https://github.com/Daily-AC/wanctl/releases/latest/download/install.sh \| sh` | `curl -fsSL https://wanctl-relay.z10.dev/install.sh \| sh` |
| Windows | `irm https://github.com/Daily-AC/wanctl/releases/latest/download/install.ps1 \| iex` | `irm https://wanctl-relay.z10.dev/install.ps1 \| iex` |

镜像不是"另一个下载站"，而是**同一个 relay 发的脚本里烧着它自己的地址**：
实测 `https://wanctl-relay.z10.dev/install.sh` 第 14 行是
`RELAY_SELF="https://wanctl-relay.z10.dev"`，于是二进制也从它的 `/dl` 取，整条链路不碰
GitHub。机制在 `internal/relay/dist.go` 的 `installerHandler` + `WANCTL_PUBLIC_ORIGIN`。

页面上用两个分段控件（系统 × 下载源）把四条收成一条：
系统按 UA 猜初值，下载源跟着页面语言（中文默认镜像），**用户手点过就不再替他改主意**。

**副作用要知道**：官网公开推荐这个 URL 之后，陌生人会从甲方的个人生产 relay 拉安装包。
它今天本来就对任何人返回 200，所以安全面没变，变的是流量。

## 3.7 收尾打磨（09-04）

- **安装命令的胶囊贴着内容走**，不是钉死在最长那条的宽度上。四条命令长短差一倍
  （44 ~ 86 字），钉死就等于给短命令留一大片空当（甲方原话「会不会太空了？」）。
  实测四态宽度 845 / 803 / 584 / 542，横向溢出全为 0。
- **分享卡片** `site/assets/og.png`（1200×630，71K）+ 完整 og/twitter meta + canonical。
  卡片本体是 `tools/og.html`，`tools/og.sh` 渲染时把它临时拷进 `site/`，
  所以它吃的是站点自己的 `app.css` 和字体——**卡片上那块手机屏和首屏那块是同一套 CSS**。
  改了视觉记得重跑 `tools/og.sh`。
  踩过一个坑：卡片里用了 `.foot` 这个类名，被站点页脚的 `background:var(--canvas-2)` 套上灰底。
- **锚点落点**：吸顶导航 48px 高，之前每一屏跳过去顶部都被压在导航条底下。
  `html{scroll-padding-top:48px}` 修掉，实测四个锚点现在都停在 48。
- **页脚补了文档入口**六条（Docs / Architecture / Run your own / Security / Releases / GitHub），
  每条 `curl` 实测 200，默认分支查过是 `main`（没手写）。这就是原计划里的「文档入口」，
  没单开一屏。
- **favicon 换了**：`mark.svg` 原来还是 LOTO 世代的表盘锁，和导航条里那把锁不是同一副几何。

## 3.8 版式换轴（09-04 下午）

甲方提出"都居中是不是看久了无感"、"两边比较空要不要加内容"。量下来
**两边空不是内容不够**：1440 的锁下实际画到最宽 845px，左右各恒定空 275–348px。
所以是布局问题，加内容是治标（且正是 09-03 密集恐惧症那条路）。

改成**海报居中 + 章节走左脊 + 交替**，规则和理由写进 `DESIGN.md` 第 5 节。
我原先说"左右交替是通用 SaaS 味"，被甲方否掉，**他是对的**——交替本身没问题，
无意义的交替才是。我们这页自带意义：首屏已定笔记本在左、手机在右，
主体落哪边就说明它属于谁。

判断单位是**一对**不是一屏（看一屏看不出交替），所以那次一起改了凭证和设备表两屏。

## 3.9 权重重排 + 两扇门 + 换标（09-04 傍晚）

- **权重**：五段等权改成三档（chapter / 默认 / minor），规则进 `DESIGN.md` 第 5 节。
  命题那一屏加了一条通栏收尾，写死「规则管一条命令，不是一台机器」——
  首屏 demo 每轮都在演，页面上原先一个字没写。
- **两扇门**（`#relay`）：官方实例 `wanctl.z10.dev`（邀请制）与自部署并置。
  **左边那扇门故意没有申请入口**——申请流程是 issue #9，而"申请是否公开"这件事
  甲方还没拍板，在这儿硬造一个入口等于替他把板拍了。地址本身是它的把手。
  #9 落地后把 CTA 补进 `.door` 即可，位置留好了。
- **换标**：挂锁退役，改成纯字标；favicon 是画成路径的 `w`。理由见 `DESIGN.md` 6.2。
  **改了标记要重跑 `tools/og.sh`**（分享卡片里也有字标）。
- 页脚跟着两扇门那屏（浅灰）翻成白底 + 一条发丝线。

## 3.10 部署（09-04，已上线）

按 `fleet-deploy`：静态文件在 **ls** `/srv/www/wc.z10.dev`，**hk 只做 TLS 终止 + 反代**。
`wc` 是一级子域、`wc.lab` 是二级，两个都命中已有泛域名，**DNS 一条都没加、证书一张都没签**。
两个名字写在同一个 server block 里（照抄邻居 `sifter.z10.dev` 的样板）：
橙云那个分享出去有 CF 边缘缓存，灰云那个给大陆访客绕开 CF 免费版的阿姆斯特丹绕道。

- ls：`/etc/nginx/sites-available/wc`（8443，证书 `/srv/hyakki/certs/`）
- hk：`/etc/nginx/conf.d/30-wc.z10.dev.conf`（`proxy_pass https://49.232.31.177:8443`）
- 发布：**`tools/deploy.sh`**

**发布脚本为什么要改文件而不是直接 rsync**：实测 CF 会用自己的 Browser Cache TTL
（免费版默认 4h）**改写浏览器看到的 `Cache-Control`，源站写 `no-cache` 也拦不住**——
下次改版，回头客最多 4 小时还在跑旧 CSS 配新 HTML。所以脚本在发布副本上给
`app.css` / `app.js` 打内容指纹（`?v=<md5 前 10 位>`），换内容就是换 URL，
nginx 那边这两类因此可以放心 `immutable`。**本地开发不带指纹，不用跑构建。**

两个实测到的坑：

- **macOS 的 `tar` 会带 `._*` 影子文件**（扩展属性），那些会被公开服务出去。
  脚本里 `COPYFILE_DISABLE=1` + `--no-xattrs` + 落地后再 `find -name '._*' -delete`。
- **`chown -R www-data` 之后 ubuntu 就读不了那个目录了**，核验（`find | wc -l`、`du`）
  必须排在 chown 之前，否则整条命令在最后一步 permission denied。

实测缓存分布：字体 `cf-cache-status: HIT`（immutable 一年，116K 全走 CF 边缘不碰 hk），
og.png / mark.svg HIT，指纹后的 css/js immutable，HTML `no-cache` + `DYNAMIC`
（CF 免费版本来就不缓存 HTML，15K 而已）。橙云首字节 ~1.6s、灰云 ~0.69s，
和 `fleet-deploy` 里记的阿姆斯特丹绕道口径一致。

**09-04 夜补：目录重定向会泄漏源站端口。** `wc.z10.dev/docs`（不带斜杠）曾被 301 到 `wc.z10.dev:8443/docs/`：
hk 反代到 ls 的 8443，nginx 给目录补斜杠时把自己监听的端口写进了 Location。修法是在 ls 的
`/etc/nginx/sites-available/wc` 加 `absolute_redirect off;`（备份在同目录 `.bak-20260904`），已重载并实测三条 URL 都 301 到干净路径。
以后加任何有子目录的静态站都要带这一行。

## 3.11 中文排版 + 手机版式（09-04 晚，issue #17，分支 `site-mobile-cjk`）

甲方在手机上报了两件事：中文一切过去**每台设备上都难看**；手机版「适配了，但看着别扭」。

**中文**：字号阶和负字距整套是给 Schibsted Grotesk 的拉丁调的，原样套到汉字上三处不对
（负字距把笔画挤在一起、1.47 行高太紧、woff2 是拉丁子集所以一句里汉字掉到系统字体
而字母数字还在 Schibsted）。修法整块锁在 `:lang(zh)` 里，`lang` 属性本来就由 `applyLang`
翻，**JS 一行没动**：换成系统 CJK 无衬线连同它自己的拉丁（macOS 的 `-apple-system`
就是 SF Pro + PingFang 这对）、字距一律 0（字标和 `.door .addr` 两处例外，都是纯拉丁）、
正文行高 1.6 标题 1.2。**标题字重维持 600**，56px 的 PingFang Semibold 实渲过，不显重。

**中文标题的断行是这一轮唯一没有纯 CSS 解的**。浏览器可以在任意两个汉字之间断开，
`text-wrap` 三种模式实测全都断词（balance 断「什|么」、pretty 断「不|会」、
默认留孤字「跑。」），`word-break:auto-phrase` 在 zh-CN 上是**空操作**（断点一个没动，
Chrome 那套分词换行目前只对日文生效，别再试）。解法是把分句包成 `display:inline-block`
的 `.cl` 盒子——盒子是原子的，断点只能落在分句之间，分句自己装不下时照样内部换行。
只包实测断得难看的两条（`#decide`、`#relay`）。

顺带一条反直觉的：**`text-wrap:balance` 在中文里宽窄两头结论相反**。宽屏它好
（首屏 lead 断在句号上）；390 下它为了拉齐把行做到 13~14 字，而那一栏放得下 19 字，
左对齐的段落右边就恒定空一条白。所以只有**左对齐**的 lead 在 ≤760 翻成 `pretty`，
居中的海报 lead 三处不动。

**手机版式**四条，全部是减法，不是加手机专用版式：

| 症状（390×844 实测） | 减掉什么 |
|---|---|
| 首屏 1102px 高、视口 844，场景下半截被折线切掉 | ≤560 隐藏笔记本。DESIGN.md 第 7 节本来就写着移动端「落地时只看得见手机那一份」秒表，那半台笔记本只承担被切掉；故事全在手机屏上。改后 817（en）/ 840（zh），430 下 884 |
| 「一行装完」那条命令可视 264px、内容 762px，三分之二滚在框外且无提示 | ≤560 胶囊竖排，命令摊开换行，复制键通栏，圆角 999 → 18（第 5 节「大面」那一档，没引入中间档） |
| 两个分段控件各自居中，左缘 87 / 100，互相不齐也不压内容锁 22 | ≤560 两条通栏，共用同一对左右缘 |
| 身份核对那两条蓝线竖排后变成两道通栏亮蓝横杠，成了那屏最重的东西，而蓝是「点我」信号 | ≤760 隐藏 `.wire` |

外加收尾那两条命令原先在连字符处断开（`--format=csv` → `--` / `format=csv`，读起来像
两个参数），按 3.5 同一个解法给每个 token 包 `.tok`（只在 ≤760 生效）。

**量具**：headless Chrome 把窗口钳到 500px 那条（第 4 节）绕法换成了 CDP 的
`Emulation.setDeviceMetricsOverride` —— 直接设布局视口，媒体查询和溢出就是手机看到的，
不用 iframe 壳也不用裁图。

**验收**：1440 英文桌面与 `origin/main` **逐像素相同**（0 / 23,834,880；量之前先把两处
活体时钟和两处墙钟文本冻住，否则会有 838px 的假差异）；390 / 430 两种语言
`scrollWidth == innerWidth` 且无元素越界；`tools/contrast.py` 与 `tools/deploy.sh --dry-run` 均过。
**Safari 没验成**：Apple Event 超时（-1712），本机没有 `safaridriver` 权限，
只能按支持度推断（`:lang()`、`inline-block`、`overflow-wrap:anywhere` 都没问题；
`text-wrap:pretty` 老 Safari 不认会退回默认换行，不比现状差）。Windows 无本机，
YaHei / Noto 那两档是读出来的不是测出来的。

**上面那条竖排安装胶囊已于 09-05 被甲方撤回**（issue #25，分支 `site-nav-install`）：
真机上换行之后「一行装完」不再是一行，通栏的蓝色复制键又把整屏最重的东西给了一个次级动作。
恢复成桌面那一份 —— 单行、胶囊内横向滚、复制键在右 —— 另加一道 24px 的遮罩当滚动提示
（是遮罩不是渐变，只擦掉末端不铺新颜色，且只挂在四条命令全都溢出的 ≤560）。
同一轮还修了顶栏在手机上溢出、语言开关被挤出屏幕：**这一节记的
「390 / 430 两种语言 scrollWidth == innerWidth」当时是真的，却漏了真机**
—— 甲方那台 412px 安卓把每个字渲宽约 10%（字标 76.4 → 84、GitHub 45.3 → 50），
四条链接那一栏 388 胀到 427 就顶出去了。所以断点重排成按「自然宽 ≤ 视口八成」定：
设备 760 → 怎么工作 + 安全 560 → GitHub 440 → 文档 360，安装和开关永不掉；
`.nav .links` 另加 `min-width:0;overflow:hidden` 兜底，**溢出时被裁的永远是链接不是开关**。
文档站顶栏同一份 `.nav`，跟着一起好了，`docs.css` 一行没动。

## 4. 已验证的事实（别重测）

- **策略语义**忠实移植自 `internal/policy` 的 `matchCommand` + `ruleMatchesShell` exec 分支：
  规则记的是**命令**不是设备。签「一直允许」后，第三轮换成 `nvidia-smi` **会重新问**。
  六条分支在真浏览器里跑过，落点全对。
- **对比度是算的**（`tools/contrast.py`）。别凭记忆写数字——本轮我写错过三个。
  `ink-3 #86868b` 实测 3.62:1，**不得承载文字**。
  09-04 把这个工具改成**直接读 `app.css` 的 `:root`**：它原先还在验 LOTO 那版早已下线的
  黄/朱红，换过调色板却没人发现。顺手补了两对从来没验过的——深色屏上的正文
  （15.46）和次级文字（6.54），都通过。
- **动效验收要用真浏览器的真时间**，别信 `--virtual-time-budget`：它下面各条动画时钟
  互相不同步，截出来的中间态是假的。用 `~/.agents/skills/visual-acceptance/scripts/cdp-drive.mjs`
  连 CDP，按真实毫秒采样 `window.__demo.state()`（已扩展出 `asleep` / `waiting` / `nudged`）。
- **headless Chrome 把窗口钳到最窄 500px**，`--window-size=390` 是按 500 排版再裁到 390，
  看起来像整页横向溢出其实是量具的锅。绕法已收编成 `tools/shot.sh`。
- **部署路径已查清，无需再研究**：`wc.z10.dev` 是一级子域，命中已有泛域名
  `*.z10.dev`（橙云 + CF Universal SSL）→ **不用加 DNS、不用签证书**。
  静态站按 2026-08-06 政策放 **ls**，hk 只做 TLS 终止 + 反代。见 `fleet-deploy` skill。

## 4.5 已归档到 GitHub issue 的待办（09-04，甲方指定「UI 打磨完之后再搞」）

- **#9** 提交申请与审核 —— `wanctl.z10.dev` 是邀请制，官网要把人送过去就得有个申请入口 +
  审核队列。**申请是否公开这件事没定，它决定用什么机制，动手前先问甲方。**
- **#10** 非管理员能通过 `#invites` 打开管理员视图 —— 纯 UI 泄漏，
  服务端 `requireAdmin` 是好的（`TestInvitesRejectNonAdmin` 断言 403 且 relay 根本没被碰到），
  所以敢开公开 issue。根因是 hash 路由不看角色，`applyView` 该拦。
- **#11** 装完第一次启动让用户选官方 relay 还是自建 —— 注意从镜像装的人已经选过了
  （脚本里烧着 `RELAY_SELF`），别再问一遍；也别在非 TTY 下阻塞无人值守安装。

## 5. 下一步（按甲方原排序）

1. **官网其余章节**。首屏已认可，往下补：怎么工作 / ~~安全模型~~ / 自部署 / 文档入口。
   节奏照 `DESIGN.md` 第 5 节的整幅明暗交替，每屏一个主体。
   **纪律：一次一段，做完给甲方看，点头再进下一段**（批量铺开被整条回滚过两次）。

   **安全模型（09-04 完成，待甲方过目）**：深色屏 `#private`，主体是一张「端到端身份」
   核对卡——两端各自的 Ed25519 指纹上下对称排开，中间一条链路向内画到一起，
   落款是中转那边的**一整行**记录 `acme  bench-02  dial  <时间>`
   （忠实于 `internal/relay/pgstore.go` 的 `INSERT INTO audit (namespace, device, event)`
   ——中转手里就这四格，没有命令、没有输出、没有文件名）。
   指纹是假的但格式真（`SHA256:<44 字 base64>`，同 `transport.Fingerprint`）。
   动效只有一次：滚进视口时两端向中间钉一下。**CSS 默认状态就是终局**，
   无 JS / 无头渲染 / reduced-motion 看到的都是已钉住的样子。

   往下还剩：**自部署**（`selfhost/` 的 compose，主体可以是那份 compose 或 relay 起来的样子）、
   **文档入口**。届时要处理屏序明暗：现在末尾是 安装（白）→ 页脚（浅灰），
   再插两屏得重排，别让两块同色相邻——颜色变化就是这套东西唯一的分隔线。
2. **门户重构** `internal/portal/`。现状盘点：
   - 单文件 `index.html` 1805 行，`//go:embed index.html` 嵌进二进制，`/` 由 `handleIndex` 发
   - 结构：CSS 427 行（其中第 435 行是 77K 字符的单行 base64 PNG，这是它没法维护的直接原因）、
     HTML 333 行、JS 1033 行、2 张 base64 内嵌图
   - **11 个视图**：devices / notify / device / tokens / friends / invites / acl / docs /
     downloads / changelog / audit
   - 约 45 个 API 端点，重写必须保住这份契约
   - 建议：拆成 `internal/portal/web/{index.html, app.css, app.js}` + 图片落成真文件，
     走 `go:embed` FS 多文件。**保持零构建**——contributor 不该为改个样式装 node 工具链
3. **部署 wc.z10.dev**（路径见上，已查清）

## 6. 还没定的

- 门户重构后**中文还是跟官网一样做双语**（现在门户是纯中文，`DESIGN.md` 第 9 节暂定纯中文）
- LOTO 那四个被否的原型（`portal-directions` worktree，8 个 HTML + 31 张截图 7.8MB）
  **要不要进公开仓库**。我倾向不进：公开 OSS 仓库里放被否的设计稿是纯体积，
  决策本身已经记在 `DESIGN.md` 第 10 节和本文第 2 节了
- 「不惊艳」那条用户反馈没解决。我查过 **`wanctl exec` 目前没有 fan-out，一次只能打一台**——
  官网摆六台但演示只用一台，这个缺口产品侧和官网侧是同一个。甲方当时的回答是
  「更多的是视觉效果」，所以先做视觉；fan-out 留着

## 7. 本轮花费

生图 25 张 × ¥0.10 = **¥2.50**（mox + gpt-image-2 + 16:9）。
